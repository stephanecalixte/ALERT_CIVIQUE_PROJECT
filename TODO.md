# TODO: Fix TypeScript 'void truthiness' Error

✅ Step 1: Update useLiveStream.ts with proper CameraView ref type and explicit null checks
✅ Step 2: Verify no other files need similar fixes using search_files (fixed useCamera.ts too)
✅ Step 3: Test TypeScript compilation and live stream functionality (expo server running)
✅ Step 4: Complete task

To test: Run `npx expo start --clear` or your TS checker. The error should be resolved with proper ref types and null checks. Live stream toggle remains functional.

